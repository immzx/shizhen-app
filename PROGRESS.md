# 拾帧 · 项目进度

> 本文件是**唯一的事实来源（Single Source of Truth）**。
> 任何一端（其他 AI、其他设备、未来的自己）接手时，只需读完本文件即可继续工作。
> 每次会话结束前必须更新本文件。

---

## 一、当前状态速览

| 项目 | 内容 |
|---|---|
| **当前阶段** | 阶段 3 · 流程与信息架构定稿（04 v0.2 初稿已产出，六项决策已确认，待整篇定稿） |
| **上一动作** | 产出《04-流程与信息架构》v0.2 并确认六项新增决策（D13–D16 等） |
| **下一个动作** | 04 整篇通读定稿 → 进入阶段 4（设计语言与动效规范） |
| **阻塞项** | 无阻塞；D10（目标 API 版本/最低机型 + PhotoViewPicker 备选验证）、D12（真实照片素材）、笔记本配置待测 |
| **代码状态** | **尚未开始**。阶段 1–4 为纯设计阶段，明确不碰代码 |
| **仓库** | https://github.com/immzx/shizhen-app （私有，已推送） |
| **最后更新** | 2026-09-04 |

---

## 二、项目是什么（30 秒版本）

**一句话**：从几千张照片里，挑出真正值得留下的那十几帧，做成一本你愿意拿在手里的作品集。

- **平台**：HarmonyOS NEXT（纯鸿蒙，无 Android 兼容层）
- **技术栈**：ArkTS + ArkUI 声明式 UI + Stage 模型
- **产品目标**（用户原话）：提供情绪价值、交互简洁、有趣、有仪式感、便于发朋友圈、可扩散、后期通过打印摄影集增值
- **核心洞察**：用户的真痛点不是「不会整理」，而是**「不舍得删」**。因此产品本质是**降低决策成本的游戏化机制**，不是相册管理工具。筛选环节的 KPI 不是准确率，而是「用户是否愿意滑到最后一张」。

**产品链路**：

```
首页（未完成任务优先）
  → 创建任务（选相册来源，先选照片后命名）
  → 出片转场（打印→落堆，峰值 1：惊喜）
  → 卡片筛选（左滑放下 / 右滑留下，单次 60 张，可分批）
  → 详情页（结果与进度共存）
  → 生成作品集（选风格 → 视频 + 长图，峰值 2：感动）
  → 分享（扩散）→ 打印钩子（变现）
```

---

## 三、接手指南：按什么顺序读

| 顺序 | 文件 | 用途 |
|---|---|---|
| 1 | **本文件 PROGRESS.md** | 了解当前进度与下一步 |
| 2 | `docs/产品定义/01-产品定义.md` | **必读**。五条产品原则、情绪地图、指标、「不做清单」 |
| 3 | `docs/产品定义/02-分享物规格.md` | 双产物（视频+长图）选型、品牌策略 |
| 4 | `docs/UX设计稿/raw-ux-0903.html` | 五个页面的交互原型（浏览器打开） |
| 5 | `docs/UX设计稿/作品集生成效果-1.jpg` | 生成阶段的效果参考图 |
| 6 | `docs/产品定义/分享物视觉样稿.html` | 朋友圈语境下的分享物预览 |
| 7 | `docs/产品定义/03-开发环境准备.md` | 装环境时读 |

**只读前两份就能开始干活。** 前两份包含所有约束条件。

---

## 四、五条产品原则（做任何决策前先自检）

| 编号 | 原则 | 含义 |
|---|---|---|
| **P1** | 决策越少越好 | 主链路决策点 ≤ 5 个；新功能默认进二级页面 |
| **P2** | 等待必须被看见 | 任何超过 1 秒的等待都必须转化为有信息量的内容，禁止孤立 Loading |
| **P3** | 措辞塑造价值 | 用「留下 / 放下」，不用「喜欢 / 不喜欢」「保留 / 删除」 |
| **P4** | 分享物是一等公民 | 任何功能不得损害最终分享物的完成度与美感 |
| **P5** | 峰值即转化 | 商业入口嵌在情绪峰值处，不做独立商城 Tab |

> 新增需求评审流程：先问「违反哪条原则」→ 若违反，需明确说明收益为何大于代价。

---

## 五、决策记录

### 5.1 已拍板（2026-09-04）

| 编号 | 议题 | 决策 |
|---|---|---|
| **D1** | MVP 一期范围 | 完整版：选片 + 长图 + 视频（视频含失败降级至长图） |
| **D2** | 滑卡分批与上限 | 单次 60 张，滑完可继续下一批，支持中断续做 |
| **D3** | 音乐版权 | 内置 3–5 首自制/免版税曲库，一期不支持本地音乐 |
| **D4** | 品牌露出强度 | 视频尾帧 1.5s + 长图 1.5% 高度 |
| **D5** | 打印业务深度 | 一期只放钩子：预约/心愿登记，不接支付 |
| **D6** | 筛选机制 | 一票否决（左滑放下 / 右滑留下），不做打分制 |
| **D13** | 候选集合上限 | 单任务候选照片 ≤ 600 张（10 批），超限提示缩小范围（来源：04） |
| **D14** | 已留下集合可编辑性 | 结果页缩略墙仅支持长按移除误留，不支持新增（来源：04） |
| **D15** | 打印登记落地 | 一期钩子卡保留、登记页 P8 暂缓；点击钩子卡仅轻提示「成册服务准备中」，不建表单（来源：04） |
| **D16** | 换风格重新生成 | 允许重生成，不动筛选结果；旧成品保留在「我的作品集」历史（来源：04） |

### 5.2 默认采用（未显式确认，可推翻）

| 编号 | 议题 | 默认值 |
|---|---|---|
| **D7** | 分享物可编辑性 | 一期不可编辑（不改文案、不换音乐） |
| **D8** | 长图与印刷稿版式 | 严格一致，共用同一套设计资产 |
| **D9** | 视频生成失败降级 | 自动降级为长图，同等仪式感呈现，禁止只弹「生成失败」 |
| **D17** | 滑卡批内排序 | 拍摄时间倒序（最新在前）（已确认，来源：04）；动效调优阶段可再调 |

### 5.3 仍待确认

| 编号 | 议题 | 阻塞什么 |
|---|---|---|
| **D10** | 目标 API 版本 / 最低支持机型 | 阶段 5 技术验证（部分动效与媒体能力有版本门槛） |
| **D11** | ~~是否有鸿蒙真机~~ | ✅ **已解决**：Mate 70 Air |
| **D12** | 真实照片素材 | 动效参数调优（出片间隔、停留时长） |
| **D18** | PhotoViewPicker 免权限备选 | 相册权限被拒时的备选路径，列入阶段 5 技术验证一并实测（来源：04） |

---

## 六、阶段进度

| 阶段 | 内容 | 状态 | 产出 |
|---|---|---|---|
| 1 | 产品定义 | ✅ 完成 | `docs/产品定义/01-产品定义.md` |
| 2 | 分享物先行设计 | ✅ 完成 | `docs/产品定义/02-分享物规格.md`、分享物视觉样稿.html |
| — | 开发环境方案 | ✅ 完成 | `docs/产品定义/03-开发环境准备.md` |
| **3** | **流程与信息架构定稿** | 🔄 **进行中** | `docs/产品定义/04-流程与信息架构.md`（v0.2 初稿已产出，待整篇定稿） |
| 4 | 设计语言与动效规范 | ⬜ 未开始 | Design Token、动效时长表、文案语气 |
| 5 | 技术可行性验证 Spike | ⬜ 未开始 | 仅验证：出片动画、滑卡手势、视频合成 |
| 6 | 数据模型与埋点方案 | ⬜ 未开始 | ER 图、埋点表 |
| 7 | MVP 切割与迭代计划 | ⬜ 未开始 | 版本计划 |

### 阶段 3 需要交付的内容（待办清单）

> 六项交付内容已由《04-流程与信息架构》v0.2 全部覆盖（2026-09-04 产出），待整篇通读定稿后勾除。

- [ ] 页面清单（每一页的职责、入口、出口）
- [ ] 全局状态机（任务状态：未开始 / 准备中 / 筛选中 / 已完成 / 已生成）
- [ ] 异常流定义（重点：视频合成失败降级、权限被拒、照片读取失败、中途退出）
- [ ] 空状态设计（冷启动首页）
- [ ] 滑卡分批的具体交互（批次切换如何过渡）
- [ ] 打印钩子的入口时机与文案

---

## 七、技术要点备忘

| 领域 | 方案 |
|---|---|
| 页面与状态 | `Navigation` + `NavPathStack`；`@ObservedV2` / `@Trace` |
| 出片动画 | `keyframeAnimateTo` 多段关键帧 + Z 序变化 |
| 滑卡手势 | `PanGesture` + `translate/rotate` 跟手，松手用 `curves.springMotion` |
| 撤销 | 栈结构回放反向动画 |
| 照片读取 | PhotoAccessKit，需 `ohos.permission.READ_IMAGEVIDEO` |
| 保存产物 | 需 `ohos.permission.WRITE_IMAGEVIDEO` |
| 视频合成 | MediaKit（**全流程技术风险最高的一环**，须优先验证） |
| 性能 | `LazyForEach` + 图片降采样，禁止一次性构建全部组件 |

### 环境硬约束（重要）

#### 主开发机实测（2026-09-04）

| 项目 | 实测 | DevEco 要求 | 判定 |
|---|---|---|---|
| CPU | AMD Ryzen 5 4600G（6核12线程，APU 核显） | i5 最低 / i7 推荐 | ✅ 够用 |
| 内存 | 8 GB DDR4 3200，**单条**（插槽 2 个仅占 1） | 最低 8 / **推荐 16** | ⚠️ 不足 |
| 可用内存（实测） | **0.56 GB**（最低见过 0.4 GB） | — | ❌ 灾难级 |
| C 盘 | 可用 22.7 / 总 120 GB | 最低 30 GB | ❌ 不足 |
| D 盘 | 可用 28.7 / 总 104.8 GB | 最低 30 GB | ❌ 不足 |

#### 三个瓶颈与对策

1. **内存**（主瓶颈）：8 GB 单条，**主板还空 1 个插槽** → 加一根 8 GB DDR4 3200 即可到 16 GB。
   另：系统只认到 7.33 GB，APU 核显占约 0.7 GB，可在 BIOS 调小 UMA Frame Buffer Size 释放。
2. **磁盘**（比内存更麻烦）：两盘单算都低于 DevEco 最低 30 GB。
   实际所需 = IDE 约 10–15 GB + SDK 约 15–20 GB + 项目与构建 5–10 GB + 系统余量 20 GB ≈ **≥ 60 GB 可用**。
3. **模拟器**：8 GB 下**不可用**，必须真机调试。

#### 结论

- **这台台式机不适合做鸿蒙主开发机**（内存 + 磁盘双瓶颈）。
- **优先方案**：改用用户那台「配置较高」的笔记本（**配置待测**）。
- 若坚持用台式机：需加 8 GB 内存条 + 腾出 ≥ 60 GB 磁盘。

#### 其他

- 真机：**HUAWEI Mate 70 Air**（HarmonyOS NEXT）✅
- DevEco 5.x 起 SDK / Node / ohpm / hvigor 已内置，**勿照旧教程单独下载 SDK**
- 8 GB 下须调低 IDE 堆：`Help → Edit Custom VM Options` 设 `-Xmx1536m`

#### 本机工具使用注意

PowerShell 工具**不返回标准输出**。查询本机系统信息时须用 `Set-Content` 写入
临时文件再用 Read 读取；Bash 中直接调 `powershell.exe` 会被安全策略拦截。

---

## 八、变更日志

### 2026-09-04

- **项目启动**。阅读 UX 设计稿（5 页交互原型 + 作品集生成效果图），确立产品理解。
- 确立项目开展方法：**逆向设计** —— 从「分享出去的那一件东西」倒推全链路。
- 提出 **7 阶段推进路线**（前 4 阶段纯设计，不碰代码）。
- 产出《01-产品定义》：价值主张、五条原则、情绪地图（双峰值）、指标、不做清单。
- 产出《02-分享物规格》：**A（视频）+ B（长图）双产物**选型。
  - 关键判断：**长图即印刷排版的前置产物**，共用同一套设计资产，把「分享」与「打印」串成闭环。
  - 钩子文案用数据不用口号：「这 15 张，是从 832 张里挑出来的。」
- 产出《分享物视觉样稿》：朋友圈语境预览 + 三种风格对照。
- 用户拍板 6 项决策（D1–D6），另 3 项默认采用（D7–D9）。
- 产出《03-开发环境准备》：针对 8 GB 内存的定制方案。
- 搭建本仓库与同步机制。
- **环境诊断**：实测主开发机配置，发现**内存（8 GB 单条 / 可用仅 0.56 GB）与磁盘（C 22.7 + D 28.7 GB）双瓶颈**，
  判定该机不适合做鸿蒙主开发机，建议改用笔记本。
- **GitHub 账号澄清**：SSH 公钥被加在 `immzx` 账号下，而 WorkBuddy 的 GitHub 连接器绑的是
  `mengshao0519-wq`（只读、无建仓权限）。确定仓库应建于 **`immzx`** 账号下。
- 产出《04-流程与信息架构》v0.2：页面清单（P1–P8 主链路 + S1–S3 二级）、任务五态状态机、
  四类异常流（视频降级 / 权限 / 照片读取 / 中途续做）、空状态设计、滑卡分批交互
  （60 张/批、批间自动衔接）、打印钩子两层分工（分享物内「我也要做」 vs 结果页「印出来」）。
- 六项决策确认（来源 04，已并入 5.1–5.3）：D13 候选 ≤600 张｜D14 仅可移除误留｜
  D15 钩子卡在、登记页暂缓｜D16 允许换风格重生成｜D17 批内时间倒序｜D18 PhotoViewPicker 列入 D10 验证。
- **新环境落地**：仓库在本机（WorkBuddy 台式机）改为 **HTTPS 直连 clone 至 `D:\拾帧`**（无 SSH；代理环境变量残留
  会拖垮 git，已用仓库级 `http.proxy=""` 豁免）。仓库曾临时转 public 以便读取，评审完成后应改回私有。

---

## 九、仓库与同步规范

### 9.1 仓库信息

| 平台 | GitHub（私有仓库） |
| 地址 | **https://github.com/immzx/shizhen-app** |
| 账号 | `immzx`（SSH 公钥与仓库均在此账号下） |
| 仓库名 | `shizhen-app` |
| 本地路径 | `D:\拾帧` |
| 默认分支 | `main` |
| 认证方式 | SSH 密钥 `~/.ssh/id_ed25519_github_shizhen`（已在 `~/.ssh/config` 中为 github.com 配置） |
| 提交身份 | `mengshao0519-wq`（仓库级配置，覆盖全局的 gitee 身份） |
| 本机现状（2026-09-04） | 本 WorkBuddy 台式机**无 SSH 密钥**；仓库经 HTTPS 直连 clone 至 `D:\拾帧`，已设仓库级 `http.proxy=""` 豁免残留代理环境变量；推送需 HTTPS 凭据（见 9.2）。旧电脑的 SSH 密钥约定仍适用于笔记本 |

### 9.2 每日同步规范（**强制**）

每次会话结束前必须执行：

1. **更新 `PROGRESS.md`** —— 这是硬性要求。至少更新：
   - 「当前状态速览」的当前阶段、上一动作、下一个动作、最后更新日期
   - 「阶段进度」表格中对应阶段的状态
   - 「变更日志」追加当日条目
2. **生成/更新的产物文件**放入 `docs/` 对应目录（代码阶段放入 `app/`）。
3. **提交并推送**：

```bash
cd "D:/拾帧"
git add -A
git commit -m "<类型>: <摘要>"
git push origin main
```

> **注意**：务必使用 `git push origin main` 的显式形式，不要用简写的 `git push`。
> 本机环境下 remote-tracking 引用（`refs/remotes/origin/main`）无法持久化写入，
> 简写形式与 `git pull` 可能异常，但**显式 push / fetch 均正常**。
> 拉取更新时用：`git fetch origin && git merge FETCH_HEAD`。

提交信息前缀约定：`docs`（文档）、`design`（设计稿）、`feat`（功能）、`fix`（修复）、`spike`（技术验证）、`chore`（杂项）。

### 9.3 在其他端接手

```bash
# 1. 克隆（需先在该机器配置好 GitHub SSH 密钥，并加入 immzx 账号）
git clone git@github.com:immzx/shizhen-app.git 拾帧

# 2. 读 PROGRESS.md（必读），按「三、接手指南」顺序读文档

# 3. 开始工作前先拉取最新（本机环境请用显式形式）
git fetch origin && git merge FETCH_HEAD
```

**关键约定**：`PROGRESS.md` 是唯一事实来源。任何一端修改进度后必须提交推送，
否则其他端会基于过时信息工作。

### 9.4 在新机器上接入（完整清单）

新机器（如笔记本）要接续工作，按此清单执行。**本节设计为自包含，照做即可接入。**

#### 前置条件

- 已安装 WorkBuddy 与 Git
- 磁盘可用 ≥ 60 GB；内存 ≥ 16 GB（推荐）或 ≥ 12 GB（勉强，须真机调试）

#### 步骤

**1. 生成 SSH 密钥**

```bash
ssh-keygen -t ed25519 -C "shizhen-dev" -f ~/.ssh/id_ed25519_github_shizhen
cat ~/.ssh/id_ed25519_github_shizhen.pub
```

**2. 把公钥加入 GitHub**

复制上一步输出的公钥，粘贴到 https://github.com/settings/keys
**必须登录 `immzx` 账号**（不是 `mengshao0519-wq`）。

**3. 配置 SSH host** —— 在 `~/.ssh/config` 添加：

```
Host github.com
  HostName github.com
  User git
  IdentityFile ~/.ssh/id_ed25519_github_shizhen
  IdentitiesOnly yes
```

**4. 验证连通**

```bash
ssh -T git@github.com
# 期望输出：Hi immzx! You've successfully authenticated...
```

**5. 克隆仓库**

```bash
git clone git@github.com:immzx/shizhen-app.git 拾帧
cd 拾帧
git config user.name  "immzx"
git config user.email "<你的 GitHub 邮箱>"
```

**6. 读 `PROGRESS.md`，按「三、接手指南」的顺序继续工作**

#### 注意事项

- **新机器上的 WorkBuddy 不自带本项目上下文。** 首次接续时，请明确提示 AI：
  「读 `PROGRESS.md`，然后继续当前阶段的工作。」
- 本项目 `PROGRESS.md` 刻意设计为**自包含**，读完即可接续，**不依赖对话历史**。
- 若需回溯讨论细节，AI 可用 `conversation_search` 检索历史对话（云端能力，跨机器可用）。
- **不要用 WorkBuddy 的 GitHub 连接器读写本仓库** —— 它绑定的是 `mengshao0519-wq` 账号，
  只读且无法访问 `immzx` 的私有仓库。所有 GitHub 操作走本机 SSH。

---

## 十、待办与问题

1. **配置选型**：主开发机（8 GB，已测，不适合）vs 笔记本（待测）—— 用 `tools/detect-specs.ps1` 检测后比对。
   若笔记本也不达标，回到主开发机加内存方案（主板有 1 个空插槽）。
2. **D10**：确定目标 API 版本与最低支持机型。
3. **D12**：准备真实照片素材用于动效调优。
4. ~~**GitHub 仓库创建**~~ ✅ 已完成：https://github.com/immzx/shizhen-app（私有）
5. **每日同步机制** ✅ 已生效，按第九节规范执行。

> 注意：WorkBuddy 的 GitHub 连接器绑定的是 `mengshao0519-wq` 账号（只读、无建仓权限），
> 而仓库在 `immzx` 账号下。**推送统一走本机 SSH**，不要用 MCP 连接器。
